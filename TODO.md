# TODO

本清单只列下一批工作。当前能力以 [PROJECT_STATUS.md](PROJECT_STATUS.md) 为准，规则确认等级以 [RULE_STATUS.md](RULE_STATUS.md) 为准。

## P0：规则证据

- 分析 `a562bd213645d8d998e47bf62bdb45de.mp4`；核对玩家“坐庄底分5分，连庄+5”与画面庄标/显示底数字段的对应关系，保留封顶未知。
- 核验开金实体归属；抢金窗口归属/放弃后不转对手已确认，不再重复追问。抢金剩余只核验精确胡型资格、胡后结算/付款及下局庄位。
- 三金倒时机已修正并确认：仅在“刚摸进第3张金”的瞬间出现【三金倒 / 过】；点【过】后该次机会消失，不能因为手里仍有3金在后续行动节点再次触发。普通胡时3金仍按+3台累计。当前代码仍是旧的3+金通用窗口逻辑，按用户要求暂不改代码、不跑测试；待本批规则收集完后统一修正。后续仍需真实三金倒结算，以及第4张金是否存在新特殊窗口的证据。
- 补齐游金、双游、三游的入口、升级、取消、响应、终局和付款。
- 核验补杠/蓄杠/加杠 `ADD_KONG` 窗口的实际响应录像；其为已有碰+自己摸第4张升级，可被抢杠。大明杠 `MING_GANG` 与暗杠 `AN_GANG` 不可抢已确认。补齐抢杠胡/杠胡计分、独立补杠费/流局杠费和庄家底分上限。

## P1：模拟与 AI

- 已完成：FanAggregator V0.1 已接入；b3892b34自动聚合4番、66fe863f自动聚合5番，金补刻不计暗刻番，点炮补成刻子也不计暗刻番；多金/花组/普通碰/拆牌歧义显式UNKNOWN。最新 Core regression 在 Python 3.10/3.11/3.12 均185项全通过。
- 最终 AI 目标改为8局后总分最大化：双方1000起步。先补齐真实单局番数/结算，再实现连续8局 Match Simulator，把当前总分、剩余局数、庄位/连庄底传给AI；评估以最终分数/分差为主，单局胜率仅作辅助。
- 已完成：`FanResult.complete=True` 的普通平胡/自摸已接入自动真实 Settlement；自动从 `HU_DECLARED` 重建结构、聚合番数并用 `(当前庄底+赢家番)×1/2` 结算，UNKNOWN 番项/杠费原子阻断。最新 Core regression 在 Python 3.10/3.11/3.12 均188项全通过。
- 已完成：`MatchRunner` 和 `run_real_ordinary_match()` 已接通真实普通单局→1000/1000总账→庄位/连庄底→下一局；UNKNOWN会在该局安全停止并保留部分比分。最新 Core regression 在 Python 3.10/3.11/3.12 均198项全通过。
- 整场基线：20场 ordinary-real 尝试，0场完整8局；共真实结算23局，平均1.15局/场。UNKNOWN主因：多金番10、杠费9，其次拆解2、普通碰1、花组1。
- 已确认待统一实现：金牌按1台/张逐张累加；金作为万能牌补顺/刻/将不额外加台；金代凑刻子不算自然暗刻，也不计入双/三暗刻台。本轮不改执行代码、不跑测试。
- 已确认待统一实现：花牌1番/张线性累加，4张=4番，四花成组不额外叠加；集齐8张花即具备八花游资格，不要求普通牌型，可直接特殊胡，也可选择【过】。若过掉八花游，8张花按基础花番合计8番继续普通胡；若直接八花游胡，则不额外加倍，沿用正常结算，庄位按正常流程。本轮不改代码、不跑测试。
- 下一步证据优先级：① 大明杠/暗杠/补杠是否有独立即时杠费、流局是否保留；② 普通数牌碰牌番；③ 多拆解同手牌的取番规则；④ 抢杠胡/杠胡等特殊结算；⑤ 八花游与游金同时满足时的优先级/过后是否再触发。
- 已完成：普通局固定牌墙自摸/点炮/16张流局、BaselineAgent、批量评估、完整配对统计、按局落盘及摘要校验重放；补杠候选→抢杠窗口→PASS成立/抢杠声明→尾摸补花，三个计分UNKNOWN分别记录。
- 2026-09-18 采用杠番表并取消补杠总阻断后，同seed 100seed×换座=200局：195完成、5 UNKNOWN，剩余全部为 `KONG_FEE_SETTLEMENT_UNKNOWN`；0超步、0循环，完整配对95/100组。
- 普通局固定seed基线已更新；下一步优先核验独立即时杠费和流局杠费，把剩余5局 `KONG_FEE_SETTLEMENT_UNKNOWN` 继续压低，同时补抢杠胡/杠胡结算。普通模拟对3+金固定走【过/继续】分支，不把三金倒收益计入普通单位奖励。

## P2：离线视觉

- 校准 `hand_region`、`draw_region`、`gold_region`。
- 标注首批万、筒、条、字、花样本并建立准确率基线。
- 置信度与多帧稳定性达标前，不接入 Executor。

## 不在当前范围

- 不实现门清、碰碰胡、清一色、混一色等不计独立番的组合番型。
- 不把 UNKNOWN 规则硬编码，不启动小程序自动点击。
