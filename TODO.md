# TODO

本清单只列下一批工作。当前能力以 [PROJECT_STATUS.md](PROJECT_STATUS.md) 为准，规则确认等级以 [RULE_STATUS.md](RULE_STATUS.md) 为准。

## P0：规则证据

- 分析 `a562bd213645d8d998e47bf62bdb45de.mp4`；核对玩家“坐庄底分5分，连庄+5”与画面庄标/显示底数字段的对应关系，保留封顶未知。
- 核验开金实体归属；抢金窗口归属/放弃后不转对手已确认，不再重复追问。抢金剩余只核验精确胡型资格、胡后结算/付款及下局庄位。
- 已实现：三金倒仅在最近有效摸牌把2金变为3金时出现一次性【三金倒 / 过】窗口；PASS后不会因仍持3金再次触发。3金普通自摸已放行；3+金点炮仍保留UNKNOWN。后续仍需真实三金倒结算，以及第4张金是否存在新特殊窗口的证据。
- 补齐游金、双游、三游的入口、升级、取消、响应、终局和付款。
- 核验补杠/蓄杠/加杠 `ADD_KONG` 窗口的实际响应录像；其为已有碰+自己摸第4张升级，可被抢杠。大明杠 `MING_GANG` 与暗杠 `AN_GANG` 不可抢已确认。玩家已确认不存在独立杠费；后续只补抢杠胡/杠胡计分和庄家底分上限。

## P1：模拟与 AI

- 已完成：FanAggregator V0.1 已接入；b3892b34自动聚合4番、66fe863f自动聚合5番，金补刻不计暗刻番，点炮补成刻子也不计暗刻番；多金/花组/普通碰/拆牌歧义显式UNKNOWN。最新 Core regression 在 Python 3.10/3.11/3.12 均185项全通过。
- 最终 AI 目标改为8局后总分最大化：双方1000起步。先补齐真实单局番数/结算，再实现连续8局 Match Simulator，把当前总分、剩余局数、庄位/连庄底传给AI；评估以最终分数/分差为主，单局胜率仅作辅助。
- 已完成：`FanResult.complete=True` 的普通平胡/自摸已接入自动真实 Settlement；自动从 `HU_DECLARED` 重建结构、聚合番数并用 `(当前庄底+赢家番)×1/2` 结算。无独立杠费后，完成过杠不会再阻断普通结算；仅真实未知番项或特殊胡继续原子停止。
- 已完成：`MatchRunner` 和 `run_real_ordinary_match()` 已接通真实普通单局→1000/1000总账→庄位/连庄底→下一局；UNKNOWN会在该局安全停止并保留部分比分。最新 Core regression 在 Python 3.10/3.11/3.12 均205项全通过。
- 更新整场基线：确认无独立杠费后，同口径20场 ordinary-real 中6场完整8局；共真实结算87局，平均4.35局/场。UNKNOWN：多拆解9、3+金点炮3、普通碰2。`KONG_FEE_SETTLEMENT_UNKNOWN` 已完全消失。
- 已完成：普通胡存在多种合法拆法时，枚举全部拆分、逐套计算完整番数并取最高总番方案；门外吃碰杠固定不可重拆。求解器若标记枚举截断则仍安全UNKNOWN。
- 已完成：UNKNOWN 证据包与规则缺口报告。停止现场会保存可复现 seed、局号、比分/庄底、牌面、花、副露、杠、pending 状态；`decomposition_concealed_triplet_choice` 额外记录每一种拆解与对应番数。新增 `python -m workspace.simulator.rule_gap_benchmark` 可直接批量输出规则缺口次数、平均已结算局数和代表样例。
- 已实现并测试：金牌按1台/张逐张累加；金作为万能牌补顺/刻/将不额外加台；金代凑刻子不算自然暗刻，也不计入双/三暗刻台。
- 已实现并测试：花牌1番/张线性累加，四花无额外；八花游集齐8花即可 DECLARE/PASS，不要求普通牌型；PASS后8花=8番继续普通胡。项目暂定八花游特殊胡×2，独立声明/结算入口已实现，并在审计事件中标记为 WORKING/项目规则。
- 下一步证据优先级：① 3+金是否允许点炮胡；② 普通数牌碰牌番；③ 抢杠胡/杠胡等特殊结算；④ 庄家底分上限；⑤ 其他真实8局中仍出现的规则缺口。
- 已完成：普通局固定牌墙自摸/点炮/16张流局、BaselineAgent、批量评估、完整配对统计、按局落盘及摘要校验重放；补杠候选→抢杠窗口→PASS成立/抢杠声明→尾摸补花，三个计分UNKNOWN分别记录。
- 历史记录：2026-09-18 在“杠费尚未确认”的旧版本中，同seed 100seed×换座=200局曾有195完成、5个 `KONG_FEE_SETTLEMENT_UNKNOWN`。该规则ID现已因玩家确认“无独立杠费”而退役，不再代表当前版本。
- 普通局固定seed基线已更新；独立即时杠费/流局杠费问题已关闭。多拆解取番已解决；当前优先补3+金点炮、普通数牌碰牌番，再补抢杠胡/杠胡结算。普通模拟对三金倒和八花游继续走已确认的PASS分支。

## P2：离线视觉

- 校准 `hand_region`、`draw_region`、`gold_region`。
- 标注首批万、筒、条、字、花样本并建立准确率基线。
- 置信度与多帧稳定性达标前，不接入 Executor。

## 不在当前范围

- 不实现门清、碰碰胡、清一色、混一色等不计独立番的组合番型。
- 不把 UNKNOWN 规则硬编码，不启动小程序自动点击。
