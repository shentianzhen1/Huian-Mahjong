# Issue #69: PR 整合与仓库卫生

> 执行状态以 GitHub Issues / 实时 PR 和 CI 为准；本文只记录整合约束，不作为规则或识别率真相源。2026-09-25 快照。

## 当前主线与依赖

- `main` 当前基线：`2fbbc0d`。只在明确审查与 CI 通过后合并，不把草稿自动视为完成。
- #104：跨原始对局公开牌样本的隔离与采集；未经批准的样本不得作为训练真值。
- #109：多牌粘连导致的弃牌河不可信区间，需 fail closed。
- #110：公开牌独立身份 shadow，保持 UNKNOWN。
- #111：普通横排副露分面；叠放杠不强行拆成普通四张。
- #112：公开副露几何、分面与来源锁定身份桥。**依赖 #110**，当前临时包含 #111 模块；前置合并后必须去重/重基线。
- #114：独立手牌数量、剩余牌数与多信号时间窗口关联。**基于 #112 的未合并代码提交 `a48919a4`**，PR 对 `main` 的 diff 包含整个前置栈，不是独立的六个新增文件；必须在 #112 整合后重基线或重建干净 PR。

## 推荐合并次序与门槛

1. 分别检查 #104、#109 的变更和 CI；它们不是 #114 运行时必需的直接前置，不因顺序而无条件合并。
2. 检查 #110 和 #111 的测试及公共 API 冲突；前置工作准备就绪后，再整理 #112 的重复分面代码。
3. #112 合并并确认源锁定身份桥的回归后，重基线 #114，只保留新增手牌数量、剩余牌数、多信号关联及其测试的净变更。
4. 对整理后的 #114 跑 Tests / Vision Regression / Evidence Contracts，审阅是否存在仅由人工布尔标记冒充独立来源验证的漏洞。
5. 真实录像必须从独立手牌 ROI、公开副露 ROI、弃牌河 ROI 产生来源一致的观察；剩余牌数 OCR 只是辅助，不可单独判定谁摸牌或吃碰杠。完成真实视频审核后才能报告端到端指标。

## 不得混淆的指标

- 静态 hand+draw 147/149、Gold 193/196 属于旧同批开发录像基线，不是整局动作准确率。
- 旧首局 378 帧严格动作评估为 4/4 弃权、4 漏检、0 可认证真阳性。
- 7 组上下区域变化是待审核候选，不是 7 次自动确认动作。
- #114 的合成测试通过仅证明失败保护与接口行为；真实录像多信号识别率仍未知。

## 变更纪律

- 不在此整合中修改 Rules、Environment、Simulator、AI、Hint、Executor。
- 保持 `CurrentAgent = V0.10`、Executor OFF，所有未确认动作与牌身份为 UNKNOWN。
- 不将私有录像、原始 SHA、用户文件路径或未批准牌面样本提交公开仓。
- 只维护一个 Issue #69 主任务；分支/PR 描述标明前置依赖，不复制多个相互冲突的进度真相源。
