# CHANGELOG archive — 2026-09-19

## 2026-09-19 — match_evidence_002：三金倒时机修正 + 双游×8直接结算确认

- 用户新增 `14.mp4` 作为重要实战证据；二进制不提交公开仓，登记 SHA256：`f24898ecee56803c09bead267150a590143c043941d3287f436ed6e89930f51d`，匿名证据ID为 `match_evidence_002`。
- 三金倒时机再次被 match_evidence_002 + 玩家确认修正：开局3+金与中途2→3金都可提示；更重要的是，【过】只关闭当前提示，不再永久关闭本局三金倒。录像约43.7秒与51.7秒出现重复PASS节点，三张金始终可见；后续自己的摸牌节点若仍恰好3金，可以再次选择三金倒。持4金后的重复提示仍UNKNOWN。
- `HuianRules.can_sanjindao()` / `sanjindao_decision()` 新增后续摸牌重检查语义；`special_windows.py` 保持同一节点PASS后不立即循环，但后续自己的摸牌若仍恰好3金可重新给出三金倒。新增“PASS当前窗口→后续摸牌重开”回归测试。
- 新录像从首个动作起可见3张金，随后仍进入游金；“游”动画出现时3金仍在手。项目明确禁止用当前金牌数量定义游金阶段。
- 同一录像随后进入双游并以2张金终局，说明升级路径不能再被概括为“永远不涉及金牌离手”；但3→2是否为双游升级的必要条件仍保持UNKNOWN。
- 终局结算页直接显示：庄底30、金牌2番、花牌1番、**双游×8**、+264/-264；严格满足 `(30+2+1)×8=264`。双游倍率证据从 HIGH_CONFIDENCE 升级为 **CONFIRMED**。
- 新增结构化夹具 `tests/fixtures/settlement_match_evidence_002_double_you.json` 和 +264 回归；`SPECIAL_OUTCOMES["DOUBLE_YOU"]` multiplier status 改为 CONFIRMED。由于统一入口/升级状态机仍未闭环，`settlement_ready` 继续保持 false。
- Issue #2/#3、RULE_STATUS、RULE_EVIDENCE_MATRIX、TODO、PROJECT_STATUS 已同步；双游终局倍率缺口关闭，P0 #3 继续只追精确状态机与响应窗口。

## 2026-09-19 — PublicState Score Reader V0.1

- 新增 `public_state_scores.py`：两侧比分ROI先做12×放大和灰度二值化，再以70/80/90三个阈值产生digits-only OCR候选；OCR后端通过接口隔离，当前实验后端为Tesseract CLI。
- 读数不直接进入比赛状态：优先寻找满足双方总分2000的候选组合；只有一侧完全无候选、另一侧又只有唯一候选时才允许用2000分守恒反推；两侧都有候选但无法守恒时直接invalid。
- 8局真实回放按5/10/15/20/25/30/40/50秒抽样，共64组比分、128个单边ROI：单阈值80裸OCR为125/128=**97.66%**；错误为1000→100、920→9200、895→空。
- 三阈值候选+2000分约束后，当前同批录像 **64/64比分对正确**：63帧直接匹配守恒组合，1帧由唯一1105反推895。明确记录：64/64是系统级同批录像结果，不等于OCR 100%，也不等于外部泛化率。
- Score Reader输出继续 `safe_for_executor=false`，多帧仍交由 `fuse_public_state()` 做共识、同局比分稳定和MatchScoreState交叉校验。
- 新增Score Reader预处理、候选解码、唯一侧反推回归；本轮 Tests 与 Vision Regression 均通过，Vision专用回归当前35项。
- 下一步：剩余牌数 / 第几局读取、新独立录像比分泛化、随后再做缩放/移动/遮挡压力测试。

## 2026-09-19 — 仓库真相源、CI与公开证据治理

- README / AGENTS / workspace/ai README / Issue #6 全部对齐到当前前沿：`CurrentAgent = MeldAwareShantenAgent V0.10`；V0.6固定主对照，V0.3消融基线。V0.11/V0.12“排除金等待后做即时score-aware”明确记为结构性不可触发，不再作为默认路线。
- README同步当前Vision事实：hand+draw严格基线98.66%，0.80阈值通过样本127/127正确；Gold当前同批录像196帧中193帧正确=98.47%；PublicState约束层已实现，但这些指标都不等于Executor可用。
- `pyproject.toml` 正式声明 `vision` 可选依赖（OpenCV headless / Pillow）；Vision CI改为 `pip install -e ".[vision]"`，消除CI与本地依赖口径漂移。
- `tests.yml` 删除重复手动Vision advisory；独立 `vision-tests.yml` 继续按Vision/dataset变更自动回归。
- 新增通用 `ai-paired-eval.yml` 手动评估workflow：支持V0.10/V0.6/V0.3/Baseline，固定牌墙、正反换座、Agent身份稳定RNG，并输出可下载JSON报告。
- 13个已经结案的 `*-once.yml` 一次性AI workflow 原样归档到 `references/ci-archive/once-workflows.md` 后从Actions目录移除。当前 `.github/workflows/` 仅保留 `tests.yml`、`vision-tests.yml`、`ai-paired-eval.yml`。
- 公开8局证据脱敏为 `match_evidence_001`：真实房号从文件路径/当前文档中移除，玩家显示名改为 `seat_0/seat_1`；比分、番数、庄底、公式和回归语义保持不变。结构化fixture改为 `settlement_match_evidence_001_8hands.json`。
- 文档真相源顺序固定：Issues/TODO用于执行，RULE_STATUS用于规则事实，RULE_EVIDENCE_MATRIX用于证据缺口，PROJECT_STATUS用于总体快照，CHANGELOG仅追历史。

## 2026-09-19 — Vision标签审计、98.66%严格基线、Gold多帧与PublicState

- 对8局真实标签执行严格留组混淆审计，发现1条人工错标：5.mp4中的4筒被标为2筒；回看ROI后修正真值。新增 `audit_labels.py` 生成低置信/模型分歧/缺独立来源覆盖的人工复核队列，禁止自动改真值。
- 修正后 same-region + leave-session-out hand+draw 基线为147/149=**98.66%**；hand 122/124=98.39%、draw 25/25=100%、大类100%。置信阈值0.80时127/149被接受且127/127正确。剩余S4→S3与M8→M2均低于0.80。
- 同口径OpenCV线性SVM实验为97.99%，低于当前模板，因此不替换主分类器。
- Gold V0.1调紧筒子圆点检测，解决压缩P4帧误计第五个圆的问题；8段录像约25时点/局共196个可检测gold帧中193帧正确=**98.47%**，8/8局多数票gold_id全部正确；P2/P4/P5圆点72/73=98.63%。该指标只代表当前同批录像时间表现，不冒充新录像泛化率。
- 新增 `public_state.py`：归一化score/status ROI、多帧共识、双方分数2000守恒、MatchScoreState交叉校验、同局分数不变、局号不倒退/跳跃、同局剩余牌不增加。异常帧只报issue，不覆盖可信状态。
- 归档 `match_evidence_001_public_state_seed.json`：8局在稳定时点的双方分数、局号、剩余牌、gold_id和分辨率，作为后续数字读取器的真实回归真值。
- Vision Regression 已覆盖PublicState约束、label audit、Gold圆点角标隔离；Executor继续关闭。

## 2026-09-19 — Vision真实评测基础设施与GitHub进度同步

- `tiles_v0_1` 新增 holdout 模板训练子集，真实准确率评估按 `source_frame` / 图片来源整组留出，测试组不进入模板库，避免同图模板训练-测试泄漏。
- 新增 `evaluate_tiles`：输出精确牌面准确率、万/筒/条/字/花类别准确率、置信阈值后的覆盖率/准确率、每牌类/每区域指标、混淆矩阵和不可评估样本明细。
- 新增多帧稳定性指标与CLI：按 `region+slot` 统计连续可见帧、多数牌、一致率、平均置信度和是否达到稳定门槛；明确“稳定≠准确”。
- 新增 `dataset_status`：先审计 approved 标签、独立来源组、牌类覆盖、跨来源重复牌类、三个ROI覆盖和可进入留组评测的样本比例；数据不足时不允许生成伪准确率。
- 当前Vision瓶颈由“缺评测工具”转为“缺足量真实人工标注数据”。所有Vision输出继续 `safe_for_executor=false`，Executor保持关闭。
- 最新主线GitHub Actions全绿：Python 3.10 / 3.11 / 3.12 Core regression **各306项通过**；Legacy baseline通过；Vision advisory本轮未手动触发。
- `PROJECT_STATUS.md` / `TODO.md` 已同步到上述最新状态；AI当前前沿仍为 `MeldAwareShantenAgent V0.10`，特殊规则P0顺序不变。

## 2026-09-19 — MeldAware V0.10 正式晋级为 CurrentAgent

- `CurrentAgent` 从 `TenpaiRiskTieBreakAgent V0.6` 晋级为 `MeldAwareShantenAgent V0.10`；V0.6保留为固定主对照，V0.3保留为牌效消融基线。
- V0.10完整继承V0.6弃牌策略，仅改变AFTER_DISCARD的CHI/PENG：比较PASS与每个合法副露后的最优强制弃牌，只有普通进攻元组严格改善时才吃/碰。
- 当前金牌在手时副露策略保守PASS，因为游金/特殊胡EV尚未完整进入该模型；KONG继续独立处理。
- 首轮100 pair / 200场：V0.10 121胜、V0.6 78胜、1平，平均配对分差+67.32。
- 独立确认100 pair / 200场：V0.10 118胜、V0.6 81胜、1平，平均+42.29，约95% CI +10.99～+73.59，平均点炮差约-0.48/配对。
- 两轮合计200 pair / 400场：239胜、159负、2平，平均配对最终分差+54.805。
- 晋级证据归档：`references/ai/2026-09-19/meld_aware_v010_promotion.md`。
- 向听/有效牌核心同时新增随机交叉验证：17张完整结构、16张听牌、金万能、固定1副露均与精确Hu求解器一致。
- 下一步AI从V0.10进入score-aware EV；未来策略必须直接击败V0.10，不得仅相对V0.6/V0.3改善。

## 2026-09-19 — TenpaiRisk V0.6 正式晋级为 CurrentAgent

- `TenpaiRiskTieBreakAgent V0.6` 正式晋级，新增稳定入口 `workspace.ai.CurrentAgent`；`ShantenAgent V0.3` 不删除，继续作为显式对照/消融基线。
- V0.6 不允许为了“安全”牺牲牌效：只有候选弃牌的普通向听、有效牌总张数、有效牌种类数三项完全相同，才用 Tenpai-Conditioned Wait Risk 替换V0.3最后的固定牌序tie-break；候选包含金牌或风险模板失败时回退V0.3。
- 风险分继续明确为“听牌条件下相对等待风险”，不是绝对放铳概率；第一代均匀未见暗手概率模型仍保持淘汰状态。
- 修正整场A/B的随机性公平性：随机seed现在跟Agent身份走而不是跟座位走；换座只改变座位，不改变随机策略内部采样流，并新增回归测试固定该契约。修正前的V0.6随机策略结果只保留诊断，不参与晋级。
- 最终晋级证据由10个互不重叠seed区间组成，共 **300个seed pair / 600场完整8局**，全部固定牌墙、正反换座、身份稳定RNG：V0.6 322胜，V0.3 275胜，3平；平均配对最终分差 **+18.9633**；配对分差SD 146.4750、SE 8.4567，约95% CI **+2.3881～+35.5385**；点炮319 vs 350。
- 晋级判定以8局最终分差及跨seed不确定性为主，点炮仅作辅助诊断。完整证据归档于 `references/ai/2026-09-19/v06_promotion.md`，并记录对应Actions run。
- 保存型Simulator评估注册 `current` 与 `shanten_v03` 两个显式Agent名称，方便后续复现和消融。
- Issue #6 已推进为“在CurrentAgent V0.6上构建score-aware EV”；未来V0.7+必须直接击败V0.6，同时保留V0.3对照，不得用UNKNOWN特殊胡猜测倍率做EV。
- 晋级后的主线Core regression：Python 3.10 / 3.11 / 3.12 **各272项通过**，Legacy基线也全绿。

## 2026-09-19 — AI整场评估、V0.4/V0.5淘汰与蒙特卡洛对手模型

- 新增公开8局比赛上下文 `MatchObservationContext`：AI可看到双方当前比分、局号/剩余局数、庄位、当前庄底和连续坐庄次数，但仍看不到对手暗牌或未来牌墙。
- `MatchRunner` 继续把每局 `win_source` 带入整场记录，新增 `deal_in_count_for()` / `win_source_counts`，可直接统计点炮与自摸来源。
- 新增 `run_paired_real_matches()` 作为8局真实计分AI统一A/B入口：Agent身份跨换座保持不变，只有同seed正反座位都完成的pair才进入比较；统一输出胜负、平均终分/分差、点炮、胡牌来源、UNKNOWN和逐场摘要。
- 用新口径重跑当前V0.3 vs Baseline：20 seed×换座40场全部完成，V0.3 38胜2负，平均1150.75 vs 849.25，平均分差+301.5；点炮12 vs 43（0.300 vs 1.075次/场）；320局中自摸265、点炮胡55，UNKNOWN=0。旧37:3/+228.25保留为历史快照。
- V0.4加入简单公开暴露风险 `risk_units=未见同牌数`；独立seed合并60场为29胜31负、平均分差约-9.97，不晋级。
- V0.5只在最后3局且领先时启用0.25风险权重；两个独立20-seed区间共80场约39胜41负，平均分差约-1.85，点炮45 vs 45，仍无收益，因此不晋级。
- 新增 `estimate_ordinary_deal_in_probabilities()` 研究基础设施：从自己暗手+公开牌河/副露推导实体未见牌池，统一抽样可能的对手暗手并估计候选弃牌触发普通点炮胡的概率；不读取真实对手暗牌/未来牌墙，执行单金/双金点炮限制。
- 第一代均匀未见牌蒙特卡洛已被100局被动校准淘汰：2809次弃牌、15次真实点炮，AUC=0.4993；真实点炮样本平均预测0%；Brier=0.0053455，差于基础率常数模型0.0053115。原因是均匀随机暗手无法代表真实摸打后接近听牌的分布。
- 新增第二代 `Tenpai-Conditioned Wait Risk`：在公开实体牌约束下生成普通听牌模板，输出候选牌补成这些模板的相对风险分；代码明确标记它**不是绝对点炮概率**。
- 第二代首批seed0–99校准：100/100局完成、2809次弃牌、15次真实点炮；AUC=0.5628，正样本平均风险0.150、负样本0.1285；风险分0的397次弃牌0点炮。该信号暂仅足够进入独立held-out，不足以直接晋级AI。
- 新增 `best_offense_ties()`：未来风险模型只允许在V0.3的向听、有效牌总张数和有效牌种类数三项完全相同时替换最终牌序tie-break，不得为了防守牺牲牌效。
- 被动校准包装器新增回归：记录风险/概率不得改变ShantenAgent V0.3原本动作。
- Core regression 已随校准与风险模型测试继续扩展；最终项数以最新主线CI为准。

## 2026-09-19 — 项目问题板与AI前沿整理

- 建立 GitHub Issues #1–#9，将剩余工作拆成：抢金、三金倒终局、游金链状态机、抢杠胡/杠胡、八花游、EV/危险度AI、Vision准确率、开金实体记账、低频终局规则。
- 重写 `TODO.md`，只保留当前未完成工作；已解决的庄底、PENG番、多金番、花番、无独立杠费、普通多拆法等不再混入待办。
- `PROJECT_CONTEXT.md` 清除旧“三金倒=持3+金任意触发”和 `ADD_KONG_SCORING_UNKNOWN` 口径；统一为2→3金一次性窗口、PASS永久关闭、第四金不重开，完成杠无独立杠费。
- `UNKNOWN_RULES` 退役已解决的 `honor_peng_fan` 与旧泛化 `sanjindao` 条目；抢杠UNKNOWN描述收窄到真实UI/付款/庄位等仍未闭环部分。
- `AGENTS.md` 开发优先级重排：先特殊结算闭环，再以 ShantenAgent V0.3 为AI基线推进EV/危险度/8局上下文，之后Vision，Executor最后。
- ShantenAgent V0.3 的40场8局A/B结果正式写入状态：37胜/3负/0平，平均终分1114.125 vs 885.875，平均分差+228.25；EfficiencyAgent V0.2继续保留为失败对照。
- `docs/huian_rules.md` 与 `RULE_EVIDENCE_MATRIX.md` 清除第四金窗口、补杠普通流程、庄底上限等已解决旧缺口，减少后续Codex读取冲突规则的风险。

## 2026-09-19 — 连庄庄底不上封顶规则确认

- 玩家进一步明确：**仅在同一庄家持续连庄的情况下，庄底不上封顶**；每继续坐庄一局，当前结算庄底继续+5，直到固定8局比赛结束。
- 新庄当前结算底仍为10；庄家一旦输掉，原连庄链结束，对手成为新庄并重新从10开始。该规则不是“整场庄底永远累加”。
- 因此若同一庄家从第1局一直连到第8局，庄底序列为：10→15→20→25→30→35→40→45。
- `dealer_base_extension` 从UNKNOWN中移除；40/45不再只是工程外推，而是玩家确认规则下的合法8局连庄值。
- 更新 Rules、Simulator、证据矩阵、项目状态与回归测试，保留match_evidence_001录像本身直接只观察到部分链的证据边界。

## 2026-09-19 — match_evidence_001完整8局真实对局校准

- 归档同一匿名证据场次 match_evidence_001的完整8局录像（第1/8至第8/8局），保存8个源文件SHA256、逐局结算、庄位/庄底和1000分总账；结构化fixture为 `tests/fixtures/settlement_match_evidence_001_8hands.json`。
- 双方1000/1000起步，8局后1113/887；逐局净分为+26/-68/+22/+24/+76/+25/-28/+36（以“何”为视角），全程总分2000守恒。
- 纠正关键庄底模型：闲家自身显示底5，但**新上庄当前结算底=10**；连庄每次+5，庄输换庄后新庄重置10。本场直接观察10→15、换庄10、再换庄10→15→20→25、再换庄10。代码 `SITTING_DEALER_BASE` 从5修正为10；庄底上限仍UNKNOWN。
- 8局全部逐项满足 `(当前庄家结算底 + 赢家番) × 胡法倍率`，败家番不抵扣。
- 第5局庄家游金：庄底15、金2+花2=4番、游金×4，实际+76；直接否定旧“游金链庄家额外×2”假设，并确认简单游金可在持2金时成立，两金仍各+1番。
- 第6局明确观察普通数牌暗杠，随后结算“杠牌3番”；普通数牌 `AN_GANG=3` 从HIGH_CONFIDENCE升级为CONFIRMED。
- 第1局再次直接确认2金可普通自摸：金2+花1，`(10+3)×2=26`。
- 新增整场回归测试，逐局校验dealer/base、结算公式、比分轨迹和最终1113/887；模拟器对高于直接证据范围的庄底仍只作+5工程外推，不把40/45视为已确认上限规则。

## 2026-09-19 — 特殊牌型倍率证据重新分级

- 玩家进一步确认：**抢杠胡跟自摸一样赔率**，因此抢杠胡倍率正式从HIGH_CONFIDENCE升级为CONFIRMED ×2；游戏内惠安规则页“抢杠×2”作为独立交叉支持。仅倍率升级，付款/庄位/终局流程仍由 `ROB_KONG_SCORING_UNKNOWN` 阻断自动结算。
- 重新交叉审阅已归档的惠安二人录像、结算夹具与游戏内规则页，明确区分“目标二人真实结算确认”和“游戏内规则页高可信倍率”。
- 游金×4继续为CONFIRMED：66fe863f真实结算 `(庄底20+赢家番5)×4=100`。
- 三游/三金游×16继续为CONFIRMED：真实结算 `(庄底35+赢家番3)×16=608`。
- 双游×8从CONFIRMED调整为HIGH_CONFIDENCE：游戏内惠安规则页明确写×8，7bc12fa/21d61237录像能看到双游中间状态，但该局继续升级到三游，没有停在双游的×8结算页。
- 抢杠胡登记×2 HIGH_CONFIDENCE候选：来源为游戏内惠安规则页；`ROB_KONG_SCORING_UNKNOWN`和`settlement_ready=False`继续保留，不能据此自动结算。
- 八花游继续使用项目临时×2（WORKING）。游戏内规则页“八花齐16”按番项证据管理，明确禁止把16番误读为八花游×16；真实八花游特殊倍率仍UNKNOWN。八花PASS后8张花=8基础番不变。
- 抢金、杠胡真实倍率继续UNKNOWN。天胡×4、天听×4仅登记为规则页线索，当前二人目标房启用/触发未确认，不进入可执行特殊结果。
- 对两个约57.47秒、事件顺序高度一致的三游证据记录增加“可能是同一局不同来源/表示”的去重约束；在未得到独立源哈希证明前，不按两份独立结算样本累计置信度。
- 更新 `SPECIAL_OUTCOMES` 与回归测试，使倍率数值、证据等级和可执行结算状态保持分离。

## 2026-09-19 — Vision 8局真实录像基线、空槽检测与Gold语义修正

- 直接复用完整8局真实回放作为 Tiles V0.1 首批真实数据源；8段视频、两种尺寸（1046×480 / 960×448）、379个去重关键帧、1137个ROI裁剪、168个approved标签、8个独立source_session、34/42个牌类。
- 评测泄漏已修正：真实门槛采用 `source_session > source_frame > image` 和 `same_region + leave-session-out`，同一录像相邻帧不得跨训练/测试。
- 新增亮色牌面主体归一化：先找最大可信亮色连通块并裁去深色UI/桌面边缘，再统一48×72特征；同时复用同一几何门槛做牌面存在检测，空draw/hand槽不再被强行分类。
- strict静态准确率由89.40%提升到**96.03%**：hand 95.24%（126样本）、draw 100%（25样本）、类别准确率98.68%；阈值0.80时127/151被接受，接受样本准确率98.43%。
- presence-aware固定窗口稳定性：hand 127/128=99.22%；draw 2/2=100%但样本不足；tile槽合计129/130=99.23%。旧的“空槽也出预测”稳定性口径废弃。
- Gold语义再次按用户截图纠正：当前目标房左侧黄色高亮牌是**正面可见的金牌**，可直接读出具体牌面（示例截图为4筒）；此前“黄色牌背marker”判断作废。当前 `gold_region` 必须按 `tile` 处理并输出 `gold_id`。`region_modes` 中的 `marker` 能力仅保留给未来其他确实只有状态图标/牌背的布局。
- 现有8个gold标签继续有效；但同一gold类别尚未跨两个独立source_session重复，所以严格 same-region + leave-session-out 下gold泛化准确率暂不可评估。后续继续积累完整回放即可，不要求必须录到开金瞬间。
- 用户进一步确认两侧玩家面板均为“头像 + 头像下方当前分数”。后续 PublicState V0.1 将头像仅作为定位锚点，不识别头像/昵称；读取双方当前分数并与 `MatchScoreState` 做总分2000守恒校验，同时接入剩余牌数和第几局/8。
- Vision Regression / Tests 已覆盖 source_session 留组、亮色主体归一化、空槽跳过、region-specific实际推理；marker模式测试仅作为通用兼容能力，不代表当前惠安目标房配置。
- 当前缺口：F1/F3/F4/F5/F6/F8/S7/S9未覆盖，F2/F7仅单session；draw还需更多连续可见序列；gold需同类跨session重复；PublicState尚未建立比分/剩余牌数/局数自动识别；仍缺不同录屏批次、缩放/移动/遮挡外部验证。
- 证据：`references/vision/2026-09-19/drive_8hand_tiles_v0_1_baseline.md`。原始视频、关键帧和ROI图片不提交公开仓库。
- Executor继续关闭，所有Vision输出继续 `safe_for_executor=false`。


按日期倒序维护。规则是否确认以 RULE_STATUS.md 为准；历史提交中的实现或注释不能自动提高规则证据等级。历史测试数量由提交差异中新增测试用例及现存记录核对，未保留的当时完整测试结果不补造。
