# 重要变更记录

## 2026-09-18 — 普通胡多拆法改为取最高总番

- 采用当前项目规则：先处理特殊胡；进入普通胡后，枚举全部合法普通胡拆分方案，包含金牌万能替换产生的全部合法组合。
- 每套拆分独立计算完整番数，最终选择**总番最高**的一套作为普通胡结算方案；同番时取枚举顺序中的第一套，仅用于确定性审计，不影响最终分数。
- 门外已经公开的吃/碰/杠副露固定，不参与重新拆分。
- 若胡牌求解器明确标记拆分枚举被截断，则不在不完整候选集上取最大值，继续以 `decomposition_scoring` 安全阻断。
- `FanResult` 新增 `selected_decomposition_index` 与 `selection_policy=MAX_TOTAL_FAN`，真实普通结算事件同步记录候选番、选中拆分与策略，方便回溯。
- 原9个 `decomposition_concealed_triplet_choice` 停止案例因此不再作为规则UNKNOWN；后续AI的出牌后对手吃/碰/杠/胡反应仍留给Environment/Monte Carlo层，不混入本次结算实现。

## 2026-09-18 — 多拆解UNKNOWN收敛为暗刻择优问题

- 无杠费规则落地后的20场 ordinary-real 基线中，原 `decomposition_scoring` 共9次。
- 已抓取并检查全部9个代表案例：均为**自摸、无副露**，多个合法胡牌拆法之间的分数差只来自“自然暗刻数量不同”；候选番示例包括5/6、7/8、5/6/8。
- 因此不再把这9例笼统记为“任意拆牌规则未知”，而细化为 `decomposition_concealed_triplet_choice`：目标房在“更多暗刻 vs 更多顺子”同时合法时，究竟自动取最高番拆法，还是采用另一固定拆牌顺序。
- FanAggregator 仍不猜答案；求解截断等无法分类的真正泛化歧义继续保留 `decomposition_scoring`。
- 证据包继续保存所有合法拆法及逐解番数，后续只需一次玩家/实战确认即可落实现。

## 2026-09-18 — 确认不存在独立杠费，8局完成率继续提升

- 玩家确认：开心惠安二人麻将没有“独立杠费”这一结算项。大明杠、暗杠、补杠/蓄杠/加杠成立时都不会额外即时收分；流局时也不存在杠费保留、返还或单独结算。
- 杠只通过既有杠番进入最终胡牌番数。普通胡若此前发生过杠，可继续按 `(当前庄底+赢家番)×倍率` 正常结算；16张流局无论此前是否有杠都统一0/0。
- 删除 `KONG_FEE_SETTLEMENT_UNKNOWN` 及其所有执行阻断：自动普通结算、观察普通结算、八花游项目结算、花牌补换、16张流局和 ordinary-real Simulator 均不再因“发生过杠”停止。
- 保留真正未知的杠类特殊结算：`ROB_KONG_SCORING_UNKNOWN`（抢杠胡）和 `GANG_HU_SCORING_UNKNOWN`（杠上胡）。
- 新增回归：补杠后可继续普通观察结算；补杠后到16张按正常WALL_16结束且0/0。历史“含杠终局必须等杠费”的旧断言已移除。
- Core regression 在 Python 3.10 / 3.11 / 3.12 均205项全通过；Legacy baseline advisory通过。
- 同口径20场 ordinary-real 复测：完整8局从3场提升到6场；真实结算局数从52提升到87；平均已结算局数从2.6提升到4.35。剩余UNKNOWN：多拆解9、3+金点炮3、普通数牌碰2。当前最大规则瓶颈已转为 `decomposition_scoring`。

## 2026-09-18 — UNKNOWN证据链与规则缺口报告优化

- 新增结构化 `unknown_evidence`：UNKNOWN停止时自动保存规则ID、state hash、完整模拟牌面、花牌、副露、弃牌、金牌、墙余量、pending Hu/Kong和最近动作。
- 普通胡若因 `decomposition_scoring` 停止，现保存全部合法拆解、候选番数及“每一种拆解→对应番数”的逐解映射，不再只留下一个规则ID。
- 单局证据补充 seed、骰子、步数、初始状态hash、牌墙hash；进入8局后再附 hand index、庄家、庄底、当时比分、剩余局数和 hand seed，可精确复现。
- `MatchRunResult.stopped_evidence` 直接暴露整场停止证据；新增 `summarize_match_rule_gaps()` 汇总UNKNOWN频次和有限份代表案例。
- 新增 `python -m workspace.simulator.rule_gap_benchmark`，可直接运行交换座位的 ordinary-real 8局规则缺口基准。2个seed×换座的端到端smoke成功：4场全部正常产出证据报告，平均已结算2.75局，四个当前主要缺口各抓到1个可复现实例。
- Core regression 在 Python 3.10 / 3.11 / 3.12 均204项全通过；Legacy baseline advisory通过。

## 2026-09-18 — 本批规则统一落地并完成回归

- FanAggregator：金牌改为1台/张累计；金作万能不消耗/不额外加台；金补刻不计自然暗刻。花牌严格1番/张；四花组无额外；八花PASS后基础花番=8。multi_gold_fan 与 flower_groups 从执行路径移除。
- 三金倒：从旧的“3+金任意节点”改为“最近有效摸牌把2金变为恰好3金”的一次性窗口；PASS后不再因仍持3金重复触发。3金普通自摸已放行；3+金点炮仍保留 three_plus_gold_discard_hu UNKNOWN。
- 八花游：集齐8花可 DECLARE/PASS，不要求普通牌型；PASS继续普通牌局；DECLARE进入独立声明阶段。项目暂定×2已接入独立结算入口，并明确写入 project_rule=True / evidence_status=WORKING，不冒充目标房真实倍率证据。
- ordinary-only 模拟对三金倒、八花游固定走PASS/继续分支，以便普通规则回归继续推进。
- Core regression：Python 3.10 / 3.11 / 3.12 均202项全通过，Legacy baseline advisory通过。
- 同口径20场 ordinary-real 复测：3场完整8局；共真实结算52局，平均2.6局/场（此前1.15）。剩余UNKNOWN：杠费9、拆解4、普通数牌碰2、3+金点炮2。多金/花组已不再造成停止。
- 下一证据优先级：独立/流局杠费 → 多拆解取番 → 普通数牌碰牌番 → 3+金点炮 → 抢杠胡/杠胡特殊结算。

## 2026-09-18 — 八花游项目暂定倍率改为×2

- 用户决定：考虑到八花游集齐8花的出现概率较低，项目中将八花游特殊胡暂定为×2。
- 该×2属于**项目人工规则/临时配置**，不是已由真实玩家结算图或目标房回放确认的规则证据。
- 已确认的真实规则部分不变：集齐8花即可特殊胡、不要求普通牌型、可PASS；PASS后8花按1番/张共8番继续普通胡；庄位按正常流程；与其他特殊牌型按通用框架处理。
- 后续若拿到真实八花游结算证据，与×2冲突时以真实证据为准。
- 继续遵循本批补规则约定：仅更新文档，不改执行代码、不跑测试。

## 2026-09-18 — 八花游并入特殊牌型通用处理

- 玩家补充确认：八花游在与其他特殊牌型发生并列、冲突或窗口处理时，不采用一套专属例外规则，而是按项目中其他特殊牌型的统一处理框架执行。
- 因此不再单独保留“八花游与游金谁优先”“八花游过后是否有专属重复弹窗”这类八花游特有UNKNOWN；若相关行为仍未确定，只归入通用特殊牌型框架的全局边界。
- 本批规则收集阶段继续只更新文档，不修改执行代码、不运行测试。

## 2026-09-18 — 八花游无额外倍数，庄位按正常流程

- 玩家确认：八花游虽然属于“集齐8花即可直接胡”的特殊触发，但**本身不额外加倍**。
- 直接选择八花游胡时，结算沿用正常胡的既有结算规则，不新增八花游专属乘数。
- 胡后庄位也按正常胜负流程处理，不设八花游专属庄位规则。
- 若八花游选择【过】，8张花仍按1番/张合计8番继续普通牌局。
- 按当前批量补规则约定，本轮只更新规则文档，不修改执行代码，也不运行测试。

## 2026-09-18 — 八花游触发与过牌处理确认

- 玩家确认：集齐全部8张花牌即具备八花游资格，不需要满足普通胡牌型，可直接作为特殊胡。
- 八花游不是强制胡；玩家可以选择【过】继续牌局。
- 若选择【过】，8张花牌不再产生额外花组奖励，只按基础花番1番/张计算，共8番，之后继续按普通牌局发展并以普通胡结算。
- 本条确认“8花触发特殊胡资格”和“PASS后8花=8番普通胡”；八花游特殊胡的倍率、付款公式、终局账务和下局庄位仍待补充。
- 按当前批量补规则约定，本轮仅更新规则文档与待实现清单，不修改执行代码、不运行测试。

## 2026-09-18 — 四花无额外叠加

- 玩家确认：花牌按1番/张线性累加；4张花就是4番。
- 凑齐春夏秋冬或梅兰竹菊一整组，不因“四花成组”另外增加番数。
- 该结论解决 `flower_groups` 的普通四花组额外番缺口；八花游仍是独立特殊胡类型，其触发、倍率以及是否与8个基础花番并存，不从本条外推。
- 按当前批量补规则约定，本轮仅更新规则文档和待实现清单，不修改 FanAggregator 执行代码，也不运行测试。

## 2026-09-18 — 三金倒触发时机二次修正（回放确认）

- 回放+玩家确认：三金倒不是“只要当前持有3+金就能在任意行动节点再次触发”；正确口径是手牌由2金变为3金、即刚摸进第3张金的瞬间出现一次【三金倒 / 过】窗口。
- 点【过】后，该次三金倒机会消失；后续即使手里仍保留3张金，也不能仅凭3+金再次弹出三金倒，可继续走普通胡或游金。
- 普通胡时金牌仍按1台/张累加，因此过掉三金倒后以3金普通自摸，金牌项目为+3台；金作为万能补顺/刻/将不消耗金牌本身台数，金补刻仍不算自然暗刻。
- 旧文档中的 `can_sanjindao = hand_gold_count >= 3` 通用资格口径已作废。当前执行代码仍保留旧逻辑；按用户要求，本批规则补充阶段只更新文档，不改代码、不跑测试，待规则收集更多后统一实现与回归。
- 第4张金是否另有独立特殊窗口暂未从本条外推，继续等待明确证据。

## 2026-09-18 — Real ordinary 8局 MatchRunner 接通

- 新增 `MatchRunner` / `MatchHandContext` / `MatchHandResult` / `MatchRunResult`：每局拿到当前手数、庄家、当前庄底、双方比分和剩余局数；已结算单局推进总账，UNKNOWN单局不改分并停止整场。
- Simulator 的 `enable_real_scoring=True` 已正式启用：普通局仍使用ordinary-only特殊规则旁路，但普通平胡/自摸改用 FanAggregator + 真实 Settlement，不再使用±1/±2单位奖励。结果同时标记 `simulation_only=True` 与 `real_scoring=True`，避免误称全规则真实房。
- 新增 `run_real_ordinary_match()`：把 ordinary-real Simulator 直接接入8局 MatchRunner，每局自动读取 dealer/current_dealer_base 并回写真实 rewards。
- 固定墙回归：普通点炮在庄底5、0番时结算+5；自然字牌暗刻自摸2番时结算 `(5+2)×2=14`。
- 整场基线：10个match seed×交换座位=20场，0场完整8局；共真实结算23局，平均1.15局/场。UNKNOWN累计：多金番10、杠费9、拆解2、普通碰1、花组1。最远样本完成7局。
- 结论：整场架构已接通；现阶段最高优先级从“写8局Runner”转为“补双金/多金番数与杠费证据”。
- Core regression 在 Python 3.10/3.11/3.12 均198项全通过。

## 2026-09-18 — 8局 MatchProgress 庄位与连庄底骨架

- 新增 `MatchProgressState`：在 `MatchScoreState` 的1000/1000总账之上，记录当前庄家、连续坐庄次数、当前手数和下一局庄底。
- 按已确认规则推进：庄家赢或流局 → 原庄继续，连续坐庄次数+1，下一局庄底+5；庄家输给闲家 → 闲家上庄，连续次数重置为1，庄底回到5。
- `apply_settled_hand(rewards, winner=...)` 只接受已经结算的零和单局结果，不推断任何仍UNKNOWN的番数/特殊胡/杠费。
- 新增2项MatchProgress回归：覆盖“庄赢→流局→庄输换庄”的连续变化，以及恰好8局后禁止继续结算。
- 最新 Core regression 在 Python 3.10/3.11/3.12 均190项全通过。
- 下一步实现真正的8局 Match Runner，让每局真实 Environment/Settlement 自动读取当前dealer/base并把rewards回写MatchProgress。

## 2026-09-18 — 普通真实 Settlement V0.1

- 新增 `HuianEnvironment.finalize_ordinary_outcome(current_dealer_base=...)`：只处理已声明的普通平胡/自摸。
- 自动链路：`HU_DECLARED → HuResult → FanAggregator → HuianObservedSettlementPlugin → END_HAND`，不再要求调用方手填 `winner_fan`。
- 点炮牌仍留在来源牌河以保持144张实体守恒；自动结算只在结构/番数分析时虚拟加入赢家手牌。
- 普通平胡使用×1，自摸使用×2；终局记录为 `AUTO_PINGHU` / `AUTO_ZIMO`，END_HAND事件保留番数组件、证据等级、候选番数、胡牌声明和最终rewards。
- 若 FanAggregator 返回多金、花组、拆解等 UNKNOWN，结算原子失败且不改变状态/事件；任何已完成杠也因独立即时/流局杠费尚未确认而返回 `KONG_FEE_SETTLEMENT_UNKNOWN`。抢杠胡、杠上胡继续使用各自UNKNOWN。
- 新增3项自动结算回归：自然字牌暗刻自摸按2番得到 `(10+2)×2=24`；同一东刻由点炮补成不算暗刻番，得到 `(10+0)×1=10`；双金番数未知时保持原子阻断。
- 最新 Core regression 在 Python 3.10/3.11/3.12 均188项全通过。

## 2026-09-18 — 点炮补成刻子不算暗刻番

- 玩家确认：点炮胡牌时，如果胡牌张只是把自己原有两张相同牌补成三张，这一组不算自然暗刻番。
- FanAggregator 已按胡牌来源处理：`WinSource.DISCARD` 且胡牌张等于该刻子牌值时，跳过普通/字牌暗刻番；同样牌型若由自摸形成自然暗刻，仍按普通1番、字牌2番计算。
- 新增自摸/点炮对照回归：同一组东东东，自摸计字牌暗刻2番，点炮补成则计0暗刻番。
- 最新 Core regression 在 Python 3.10/3.11/3.12 均185项全通过。

## 2026-09-18 — FanAggregator V0.1 真实番数聚合器

- 新增 `huian/rules/fan.py`：`FanAggregator`、`FanComponent`、`FanResult`。番数层与胡牌资格、最终Settlement保持分离。
- V0.1 自动聚合当前有证据支持的项目：单金1番、每张花1番、普通自然暗刻1番、字牌自然暗刻2番、字牌碰1番（HIGH_CONFIDENCE）以及当前采用的三类杠番表。
- 金牌补成的刻子不会被误算为自然暗刻番。
- 多金、完整四花组/八花、普通碰牌番、拆牌取番歧义不会默认为0；`FanResult` 返回明确 unresolved，并保留可审计的 `accounted_fan`、组件证据等级及候选番数。
- 直接以归档真实对局回归：b3892b34 自动得到 `金1+花2+暗刻1=4番`；66fe863f 自动得到 `金1+花1+暗刻1+补杠2=5番`。
- 新增7项 FanAggregator 回归；Core regression 在 Python 3.10/3.11/3.12 均184项全通过。
- 下一步仅把 `FanResult.complete=True` 的普通平胡/自摸接入真实 Settlement；任何未解决番项继续安全停止，不提前替换成猜测分数。

## 2026-09-18 — 杠番表先行确定并收窄补杠UNKNOWN

- 按现有真实结算、惠安游戏内规则页和玩家确认，当前工程先采用杠番表：普通大明杠2番、字牌大明杠3番；普通补杠/蓄杠/加杠2番、字牌补杠3番；普通暗杠3番、字牌暗杠4番。后续如出现更高优先级真实视频冲突，再修正对应格。
- 其中普通牌补杠2番已有66fe863f真实录像直接确认；其余格保留HIGH_CONFIDENCE证据等级，但运行时按当前规则使用。
- 新增 `HuianRules.kong_fan()` / `KongFanResult`，返回杠型、牌种、番值和证据等级。
- 普通 simulation-only 不再因为“已经发生过补杠”就整体停止；补杠完成后继续正常摸打、普通胡与策略评估。
- 剩余杠类UNKNOWN收缩为：抢杠胡结算 `ROB_KONG_SCORING_UNKNOWN`、杠上胡结算 `GANG_HU_SCORING_UNKNOWN`、独立即时杠费/流局杠费 `KONG_FEE_SETTLEMENT_UNKNOWN`。
- 同seed 0–99、交换座位共200局重新评估：195局完成、5局UNKNOWN，完成率97.5%，0超步、0循环；5局全部为 `KONG_FEE_SETTLEMENT_UNKNOWN`。完整配对95/100组。
- Core regression 在 Python 3.10/3.11/3.12 均177项全通过，0失败、0错误。

## 2026-09-18 — 杠型命名与抢杠范围澄清

- 玩家进一步明确目标房术语：**补杠 = 蓄杠 = 加杠**，即已经碰过三张后，自己摸到第4张再把原碰升级成四张；代码统一对应 `ADD_KONG`。
- `ADD_KONG` 属于明杠的升级形态，并且是目标房**唯一允许被抢杠**的杠；声明后进入 `ROB_KONG_WINDOW`。
- `MING_GANG` 在工程命名中专指**大明杠**：对手打出一张，自己手里已有三张同牌，直接杠成四张；大明杠不能被抢。
- `AN_GANG` 对应暗杠；暗杠不能被抢。
- 此次只解决“杠型分类与抢杠权限”，不把三种杠的番数、独立杠费、抢杠胡/杠胡结算或流局杠费视为已确认；这些计分问题继续保持 UNKNOWN。
- 代码逻辑本来已经是 added-kong-only 抢杠，本轮主要修正文档/注释/测试命名，避免“明杠不可抢”与“补杠属于明杠且可抢”产生术语冲突。

## 2026-09-18 — 8局最终得分成为AI主目标

- 玩家补充确认：默认2人8局，开局双方各1000分；每局结算的净得失在两人之间相扣并累加，第8局结束后的双方最终分数是整场结果。
- 项目评价目标从“提高单局胜率/单局平均奖励”调整为“最大化8局后的自身最终分数（以及等价的零和分差）”。单局胡牌率、胜率、平均单位奖励仅保留为诊断指标。
- 新增 `MatchScoreState`：固定1000/1000开局、总分2000守恒、最多8局、逐局应用已结算零和 `rewards`，可读取当前分数、已打/剩余局数和双方分差；`score_eight_hand_match()` 聚合完整8局。
- Match层不猜未确认的单局结算。当前 simulation-only 的平胡±1/自摸±2不能冒充真实1000分房间总分；补杠/特殊胡结算未补齐前，真实8局EV训练仍保持阻断。
- 后续AI需要把“当前总分、剩余局数、庄位/连庄底”作为比赛上下文：领先时和末局落后时的风险偏好可以不同，优化的是整场期望终分而不是每一局都追求相同胜率。
- 新增4项 Match 总分回归；最新 Core regression 在 Python 3.10/3.11/3.12 均175项全通过，0失败、0错误。

## 2026-09-18 — 三金倒时机确认与模拟基线更新

- 玩家确认：只要当前手牌持有3张或以上金，即可立即选择三金倒；三金倒不是强制胡，玩家可选择【过/继续】，牌局继续发展成其他合法牌型。
- 三金倒分支优先于抢金；3+金时不再把抢金作为并列竞争选项。三金倒真实结算、付款与下局庄位仍保持 UNKNOWN。
- Environment 新增 `SANJINDAO_DECLARED` 声明阶段；声明后只在 `sanjindao_settlement` 安全停止。PASS/继续则恢复正常牌局流程。
- 普通 simulation-only 基线对3+金固定选择 CONTINUE 分支，不把三金倒收益混入普通单位奖励；`sanjindao_timing` 已从 UNKNOWN 分类移除。
- 同一批 seed 0–99、交换座位共200局重跑：188局完成、12局 UNKNOWN，剩余全部为 `ADD_KONG_SCORING_UNKNOWN`；0超步、0循环。完整配对88/100组，配对模拟平均奖励 Random=-1.358、Baseline=+1.358。
- 为支持16张节点“过后继续摸牌”，保留原有语义位置循环检测，并对 `PASS_QIANGJIN` 这一合法的无实体牌变化动作单独放行；其他动作仍必须产生状态进展。
- 针对性三金倒/普通模拟测试已通过；一次性 benchmark workflow 已删除。随后修复动作鉴权过宽问题：完整 Action（含 metadata）必须严格匹配，`False` 不再可冒充整数索引，错误摸牌来源和伪造补杠/抢杠引用会被拒绝。最新 GitHub Actions Core regression 在 Python 3.10/3.11/3.12 均为171项全通过，Legacy 两套 advisory 也通过；Vision advisory 本轮未手动运行。

## 2026-09-18 — 抢金窗口与明/暗杠不可抢规则同步

- 抢金窗口改为“当前行动玩家独占”：开局补花/开金完成后，以及后续摸牌/补花/杠结束节点，只检查当前行动玩家；对手不会因为自己满足条件而获得同一回合的抢金窗口。
- 新增 `QIANGJIN` / `PASS_QIANGJIN` 动作与 Environment 转移。当前玩家选择【放弃】后，本回合抢金窗口立即关闭，不会把机会转交给对手；17张行动节点回到出牌，16张行动节点继续正常摸牌流程。
- 三金倒在同一特殊节点保持优先于抢金；但抢金的精确胡型判定、实际结算、付款与下局庄位仍为 UNKNOWN。当前 `working_qiangjin_eligible()` 只是工程窗口门槛，代码中的倍率元数据不得视为规则证据。
- 玩家确认：明杠、暗杠不可抢杠；只有补杠保留抢杠响应窗口。此前批量评估中大量 `rob_kong` UNKNOWN 因此成为历史数据，需要重跑同 seed 才能得到当前完成率。
- 最新相关远端提交包括 `1003fd71`、`16334b64`、`223165f9`、`b971a525`、`11e451be`、`49f70ecb`、`0fd6c989`。
- 测试边界：9月18日新增了针对抢金窗口/PASS/16张节点/开局检查的回归代码，但远端当前没有 GitHub Actions 运行记录；最近一次完整全量测试记录仍是 9月15日的 199 项通过，不能把新提交写成“已完成全量回归”。

## 2026-09-15 — 补花自动派发的观测规则

- 登记玩家确认：补花由小程序自动派发，界面没有明显逐张派发动画；Environment 将其保持为系统自动事件，不需要玩家动作。
- 明确 Vision/Recorder 不依赖补花动画，后续以手牌数量、花牌数量、牌墙剩余和开金阶段的稳定状态变化识别补花。
- 规则含义不变：补牌仍来自牌尾，既有补花顺序及连续花处理不变。本轮只更新文档，没有修改 Rules、Environment、Simulator、AI、Vision 或 Executor 代码。

## 2026-09-15 — 补杠专用抢杠窗口与计分停止分类

- 新增 `ADD_KONG`：仅已有碰副露且手牌有第四张同牌时可选，金牌禁止补杠；默认普通局启用，可通过 `enable_added_kong=False` 隐藏新候选。
- 补杠先进入 `ROB_KONG_WINDOW`，原碰及第四张手牌保持不变。对手满足普通非自摸胡限制时可选 `ROB_KONG_HU`，也可PASS；PASS后按原副露索引完成补杠，再从 `wall_tail` 摸牌，连续花复用现有流程。Baseline优先选择合法抢杠胡并记录理由。
- 抢杠胡记录 winner、loser、robbed_tile、kong_player、source=rob_kong，原碰保留；补杠尾摸胡记录Gang-Hu来源。实体牌始终只有一份，待处理结构仅保存引用；事件可确定性重放。
- `SimulationResult.unresolved` 精确区分 `ROB_KONG_SCORING_UNKNOWN`、`ADD_KONG_SCORING_UNKNOWN`、`GANG_HU_SCORING_UNKNOWN`；已发生的计分阻断优先于末步max_steps。补杠后的16张边界不会伪造零分流局，观察结算或导入终局也不能绕过未知计分。
- 范围按本轮用户要求执行，未新增抢杠实局证据；明杠/暗杠抢杠范围、独立杠费、流局杠费和真实抢杠/杠胡结算继续UNKNOWN。未接Vision、Executor或特殊胡状态机。
- 验证：全量199项通过（项目161、Core9、Environment9、Recorder14、Vision6），新增25项补杠回归。100seed交换座位200局：85完成、115 UNKNOWN（明杠/暗杠rob_kong105、补杠计分7、三金倒时机3），无超步/死循环；完整配对17组。随机样本未出现的抢杠胡和杠后胡分类由固定夹具覆盖。报告为simulation-only，不能代表真实胜率。

## 2026-09-15 — 仓库卫生（不改业务逻辑）

- 修正 `START_HERE.txt` 笔误，使入口文档与当前普通局闭环/评估能力一致。
- 更新 `AGENTS.md` 开发优先级：先补 UNKNOWN 证据与评估口径，不再把 Simulator V0.1 当成未完成任务。
- CI 核心回归补齐 `requires-python`，增加 Python 3.10。
- 补齐 `.gitignore`、`.editorconfig` 与可选 ruff/black 配置；不启用风格检查作为必要 CI 门槛。
- 规则变更：无。未修改 Rules、Environment、Simulator 或测试用例。

## 2026-09-15 — 视频证据补充与评估归档复现

- 归档 b3892b34 和 66fe863f 的源哈希、21张精选帧、时间线和结构化结算夹具；原视频及批量抽帧保留在本地忽略目录。前者确认一金三门齐全普通自摸与庄家 `(30+4)×2=68`；后者记录碰后补杠、吃后弃一条留单金、再游金的实际路径，并区分番项5、显示40与净分100。
- 收窄额外庄家×2的适用证据：普通自摸样本不另乘；游金链该倍率仍为待直接核验的玩家反馈。本局一筒补杠的2番不能推广为独立杠费或流局杠分。
- 登记玩家原话“坐庄底分5分，连庄+5”；每次连庄+5已确认，初始坐庄字段与画面显示值映射留待 a562bd21 视频核对；该新片尚未分析。
- 新增评估落盘、完整配对统计与单局校验重放；支持进度、中断记录、防覆盖、源码/运行时/事件摘要验证；本地评估输出由Git忽略。
- 验证：全量174项通过（项目136、Core9、Environment9、Recorder14、Vision6）。手动100seed交换座位共200局：81完成、119 UNKNOWN，无超步/死循环；完整配对16组，配对模拟奖励Random/Baseline为-1.6875/+1.6875。第1号记录已成功复现。结果不代表真实麻将胜率。

## 2026-09-15 — 普通局固定牌墙、批量评估与 BaselineAgent

- 新增完整144张固定牌墙的普通自摸/点炮终局回归、局中固定牌墙的16张流局回归，检查末步结算、合法动作、牌数守恒和事件哈希链。
- 新增 `run_many_normal_hands` 与手动benchmark命令，支持每seed摘要、交换座位、按座位/Agent统计胜负及奖励；UNKNOWN、max_steps和循环停止独立统计，均值只使用已完成局。
- 新增可解释BaselineAgent：能胡必胡、保留金/对子/搭子、优先弃孤张、可选吃碰先PASS。Agent只接收自己的手牌与公开信息，理由单独记录，不修改合法动作元数据。
- 修复SimulatorConfig未接入运行入口、模拟终局缺少胡牌来源/前置哈希的问题；真实配置拒绝simulation-only结算；已结束夹具不能重复当作新对局。
- 规则边界不变：普通平胡±1、自摸±2仅是模拟单位；三金、八花、显式游金状态、抢杠/补杠窗口继续UNKNOWN；未接Vision、Executor或EV。
- 验证：全量152项通过（项目114、Core9、Environment9、Recorder14、Vision6）。默认10局smoke及固定seed交换座位回归；手动CLI的seed 0–9交换座位共20局，7局自摸完成、13局UNKNOWN（抢杠12、补杠1），无步数超限/死循环。这些样本不能代表真实游戏胜率。

## 2026-09-15 — 普通局 Simulator 闭环

- 新增功能：simulation-only 普通局可跳过抢金检查，使用 RandomAgent 驱动已确认的普通摸打、吃碰、普通胡与16张流局；结果记录事件、赢家来源、UNKNOWN 原因和零和 rewards。
- 规则边界：抢金、三金倒、游金链、八花游、抢杠、补杠及真实番数不在此模式中实现。平胡 +1/-1、自摸 +2/-2 仅是模拟单位，不能代表真实最终计分。
- 测试变化：默认 20 局 smoke；100+ 局改为手动 benchmark，所有对局均有 max_steps 防死循环。

## 2026-09-15 — 工程入口、打包与 CI

- Commit：随本轮提交。
- 新增功能：README 改为入口页，包含 5 分钟上手、能力边界、架构、测试和状态文档导航；新增 Python 3.10+ 的打包声明与 GitHub Actions 核心回归。
- 修复内容：TODO 改为与当前状态一致的短清单，删除“M3 Simulator remains pending”等过期表述；本地录像分析派生图片默认忽略，避免误提交。
- 规则变更：无。RULE_STATUS.md、RULE_EVIDENCE_MATRIX.md、PROJECT_STATUS.md 继续分别作为规则、证据和进度真相源。
- 测试变化：未改业务代码；全量 128 项通过（项目 90、legacy Core 9、legacy Environment 9、Recorder 14、Vision 6），0 失败、0 跳过。

按日期倒序维护。规则是否确认以 RULE_STATUS.md 为准；历史提交中的实现或注释不能自动提高规则证据等级。历史测试数量由提交差异中新增测试用例及现存记录核对，未保留的当时完整测试结果不补造。

## 2026-09-15 — 三金游状态统一与游金计分项

- Commit：随本轮提交。
- 新增功能：新增 `YoujinStage` 规范枚举；`SANJIN_YOU` 是 `TRIPLE_YOU` 的同值别名。新增 `YoujinScoreTerms`，按`(当前庄底+已审计赢家番)×游金倍率×庄家赢家倍率`计算已确认总项。
- 修复内容：清除“三金游与三游关系未知”的过期记录；文档统一说明三金游就是三游，三金倒是另一种独立结果，AI 后续应比较两条路径。
- 规则变更：玩家确认三金游=三游、三金倒是独立×3；真实对局报告确认三游×16及`(庄底35+赢家番3)×16=608`，其中金牌项目1番、两张花2番。花番位于胡法倍率内，替换同日较早的“花水乘法后单独加”说法；庄家赢家另×2保持玩家确认。多金番、付款、声明窗口和完整触发步骤仍为UNKNOWN。
- 证据：新增`references/gameplay/2026-09-15/7bc12fa_video_evidence.md`。项目中尚未找到视频二进制，未伪造校验值或时间点，当前按用户从真实对局视频提供的逐项核验登记。
- 测试变化：新增别名统一、三金倒×3、游金4/8/16、庄家×2及三游608公式回归；全量128项通过（项目90、legacy Core 9、legacy Environment 9、Recorder 14、Vision 6），0失败、0跳过。

## 维护方式

每轮重要开发完成且完整测试通过后，追加日期、commit 或可解析的提交引用、新增功能、修复、规则及测试变化，并更新 PROJECT_STATUS 当前快照。无变更的栏目写“无”；未验证的状态明确标注。提交前记录真实测试，提交并 push 后核验远程 HEAD。历史条目保留，只有发现事实错误时作明确更正。
