# 项目当前状态

更新日期：2026-09-14（Asia/Shanghai）。只保留当前最新真实状态，历史见 [CHANGELOG.md](CHANGELOG.md)。规则状态唯一依据为 [RULE_STATUS.md](RULE_STATUS.md)；实现存在或测试通过不等于规则已确认。

## 当前版本 / 里程碑

- 当前阶段：M2 已确认局中流程可用；M3 Simulator V0.1 仅有骨架，完整牌局未完成。
- 采集验证器 V0.1 已实现。新增独立补花函数、临时房间配置和确定性开局规划器，尚未贯通 Environment。
- 最新已核验代码 commit SHA：`17bbcb54fcd25461c6c590967a1634dc60c37523`（Add deterministic Huian opening planner）。这是本状态快照的代码基线，不是包含本文件的文档提交 SHA。
- 远程：`https://github.com/shentianzhen1/Maj.git`；分支：`main`。
- 本文档提交自身的 SHA 在提交前不可知，使用 `git log -1 --format=%H -- PROJECT_STATUS.md CHANGELOG.md` 查询；仓库实时 HEAD 使用 `git rev-parse HEAD`。不填写虚构或自引用 SHA。

## 已完成模块（注明边界）

- Rules：HuianRules / HuianRulesAdapter、普通结构胡听判断、金牌副露限制、单金平胡配置；A/B 结算样本测试。结算公式仅可显式启用高置信假设，完整自动算番尚未实现。
- Environment：144 张实体牌记账、合法动作校验、吃碰移牌后强制弃牌、普通头摸、已解决杠后的尾摸、原子提交、clone/checkpoint/rollback、死循环与步数保护。
- 已确认流程：PASS 后下一家进入摸牌；头摸或已解决杠后尾摸从 17 降至 16 张立即流局，奖励 `[0, 0]`；合法 16 张局中导入结束，少于 16 张非终局导入拒绝。
- Simulator 骨架：按 seed 生成牌墙、结果结构、随机动作接口、开局未解决时返回 UNRESOLVED。未执行完整动作循环，未提供批量成绩。
- 独立规划函数：庄家优先分轮补花、17/16 张确定性发牌、骰子定位金牌指示牌。函数级测试通过；具体规则适用性、物理记账和环境集成仍待解决。
- Vision 采集：WGC / PrintWindow / 屏幕区域、预览、PNG 和 AVI 保存及元数据、黑屏/停帧/尺寸变化保护。用户已反馈三种模式可用；未完成所有遮挡、缩放及长时间运行验收。
- Legacy Core、Environment 和 Vision 文件保留，不能将其旧评分逻辑当作最终惠安规则。

## 正在开发 / 尚未完成

- 本轮：建立状态快照和变更日志维护约定，仅新增文档，不改业务逻辑。
- 后续开发主线：先统一规则依据与临时配置，再集成开局、补花、胡牌与结算，形成完整牌局循环。
- AI 强度评估、牌面识别、按钮识别、自动点击均未接入新流程；不存在已验证胜率提升。

## 当前测试结果

2026-09-14 提交前完整自动测试：**90 项通过，0 失败，0 跳过**。使用项目 `.venv-capture/Scripts/python.exe`，所有命令均加 `-B`，独立套件分别运行，退出码均为 0。

| 工作目录 | 命令（解释器后参数） | 结果 |
|---|---|---|
| 项目根目录 | `-B -m unittest discover -s tests -v` | 63 通过 |
| `legacy_code/core_v0.1.1` | `-B -m unittest discover -s tests -v` | 9 通过 |
| `legacy_code/environment_v0.1` | `-B -m unittest discover -s tests -v` | 9 通过 |
| 项目根目录 | `-B -m unittest workspace.vision.capture_validator.test_capture -v` | 9 通过 |

覆盖固定牌墙、实体牌守恒、第五张牌/非法动作拒绝、边界流局零和、回滚重放、未知分支阻断，以及真实 PNG/AVI 编解码。临时配置和开局函数的测试只证明其现有行为，不验证现实规则正确性。没有完整小程序自动对局端到端测试，也没有 AI 对战评估；legacy Vision 未发现独立自动测试套件。

## 已知问题

1. `online_room_v01.py` 默认动态留牌 `16 + 明杠数 + 2×暗杠数`，与 RULE_STATUS 当前固定 16 张流局依据未统一；只能视为临时/待核验配置。
2. 同一临时配置禁止任何弃金胡牌，而 RULE_STATUS 保留“胡牌规则允许时可胡”的条件。其代码中的 CONFIRMED 标签不能覆盖 RULE_STATUS。
3. 两人补花函数和骰子开金位置已有实现，但 RULE_STATUS 仍将具体顺序、翻花重开及物理记账列为未解决。开局逐张交替发牌明确是模拟约定；指示牌未移出牌墙，尚未确定其实际归属。
4. Environment 抽花仍停在 NEED_FLOWER_REPLACE；开局停在 READY，特殊胡牌及自动获胜结算未接入。新宣杠依赖显式无抢杠实验假设，不能当成正式抢杠规则。
5. Simulator.run 未驱动 agent/step 循环，RandomAgent 使用全局随机源，run 的 seed 尚未控制其动作随机性；广泛捕获异常可能把程序错误归类为 UNRESOLVED。不能宣称完整模拟可复现。
6. TODO 与部分 README 未覆盖全部最新辅助代码，采集 README 的待复测文字也落后于用户反馈。本轮仅新增两份文档，不修改这些文件。
7. 结算公式仍是高置信假设；新增结算图片已归档，但 RULE_STATUS 仍要求第三个验证样本，当前 A/B 回归不能替代该核验。

## 待确认规则

以下按 RULE_STATUS 的未解决事项及代码 UNKNOWN 清单汇总；PASS 和固定 16 张流局已经确认，不再列为 UNKNOWN。

- 抢杠范围、每类杠的响应窗口和结算；加杠流程及计番是增量还是总值。
- 三金倒资格、宣告时点、选择继续后的权利。
- 游金进入边界、双游能否直接进入、三游升级顺序、对手权限、取消条件。
- 双人房完整倍数链；规则页 4/8/16 及外层 ×3 不可直接用于双人净结算。
- 发牌顺序、两人补花排程、开金骰子索引、指示牌实体归属、翻花的补花及重新开金过程。
- 天胡相对补花/开金的时点；天听定义。
- 庄3及以上底分递增、封顶；8 局平局处理。
- 花组计分与叠加；金/暗刻/杠的边界；明刻是否计番、字牌碰番；八花是否另有特殊胡法及倍率。
- 多种胡牌分解的计分选择；三张及以上金的普通点炮胡；完整合法动作集、胡牌宣告及自动结算集成。
- 第三个实际结算样本对“当前庄家底分 + 赢家番数，再乘胡法倍率”的验证。

当前目标房：2 人、8 局、勾选单金不平胡、无托管。2026-09-13 未勾选截图仅记录选项页面，不能覆盖 RULE_STATUS 中最终房间选择。

## 下一步计划

1. 核验动态留牌、弃金胡与开局补花证据冲突；在 RULE_STATUS 明确依据后再调整实现，未知项不静默采用临时配置。
2. 为确认后的开局与补花准备实局回归并接入 Environment，保留实体牌守恒、合法性和零和检查。
3. 完成普通胡牌宣告与自动结算，推进 Simulator 完整循环、独立随机源、日志重放、批量及交换座位评估。
4. 建立基础 AI 后量化评估，再接入视觉识别和确认点击。

## 后续维护约定

每轮开发完成后先运行上述全部自动测试；通过后更新本文件当前模块状态、问题、规则边界、测试日期/数量及最新已核验代码 SHA，同时在 CHANGELOG 追加重要改动。失败必须如实记录并先解决，不写成通过；规则变更以 RULE_STATUS 为准。

提交前核对文档与实际代码、测试相符，再 commit 和 push；推送后检查远程分支 SHA。文档以提交前已存在的代码 SHA 标识基线，文档自身 commit 用 Git 查询，避免无限自引用更新。这是后续开发必须执行的维护流程，本次没有安装后台任务或自动 Git hook。
