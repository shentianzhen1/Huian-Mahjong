Continue the existing Huian Mahjong repository; do not restart or rewrite it.

Before touching code:
1. Fetch GitHub main and check its HEAD, Open Issues, Open PRs, and latest Actions. Reconcile your local checkout; do not assume this prompt reflects current versions or progress.
2. Read AGENTS.md and the *selected* GitHub Issue. Use TODO.md only as an Issue index.
3. For rules/settlement work, read RULE_STATUS.md and RULE_EVIDENCE_MATRIX.md. PROJECT_STATUS.md is the integrated snapshot, not a duplicate backlog.
4. Follow docs/development_workflow.md: one focused Issue slice / one small PR, explicit source lineage, UNKNOWN handling, relevant tests, and merge gates.

Use the smallest relevant local tests first. Core regression:
  python -B -m unittest discover -s tests -v
Run the corresponding GitHub Actions gate (core, Vision, or Evidence Contracts) before claiming a change is complete.

Never invent Huian Mahjong rules, silently upgrade machine observation to CONFIRMED evidence, reuse revealed Vision development sessions as independent promotion data, or use UNKNOWN special settlement as official AI reward. Do not start a new Agent line until the shared scoring blockers in the current Issues are closed. Hint Alpha is read-only until independently validated; Executor remains off until a separate post-action safety gate passes.

At handoff report: selected Issue, exact base/head SHA, files changed, tests actually run and their results, remaining UNKNOWNs, and the next concrete blocker. GitHub Issues remain the execution truth.
